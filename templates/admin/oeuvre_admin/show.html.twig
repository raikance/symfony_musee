{% extends 'navigation/admin/nav.html.twig' %}

{% block title %}Oeuvre{% endblock %}

{% block contents %}
    <h1>Oeuvre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oeuvre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ oeuvre.nom }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ oeuvre.auteur }}</td>
            </tr>
            <tr>
                <th>Creation</th>
                <td>{{ oeuvre.creation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_oeuvre_admin_index') }}">back to list</a>

    <a href="{{ path('app_oeuvre_admin_edit', {'id': oeuvre.id}) }}">edit</a>

    {{ include('admin/oeuvre_admin/_delete_form.html.twig') }}
{% endblock %}
